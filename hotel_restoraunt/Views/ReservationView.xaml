<Window x:Class="hotel_restoraunt.Views.ReservationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Бронювання" Height="400" Width="500">
    <Grid Margin="10">
        <StackPanel>
            <ComboBox ItemsSource="{Binding Guests}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedGuest}" Margin="5" />
            <ComboBox ItemsSource="{Binding Rooms}" DisplayMemberPath="Number" SelectedItem="{Binding SelectedRoom}" Margin="5" />

            <DatePicker SelectedDate="{Binding CheckInDate}" Margin="5" />
            <DatePicker SelectedDate="{Binding CheckOutDate}" Margin="5" />

            <Button Content="Забронювати" Command="{Binding AddReservationCommand}" Margin="5" />
        </StackPanel>
    </Grid>
</Window>