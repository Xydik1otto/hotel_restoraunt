<Window x:Class="hotel_restoraunt.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid>
        <TextBox x:Name="UsernameTextBox" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Margin="10,50,10,10" 
                 Text="{Binding Username}" VerticalContentAlignment="Center">
            <TextBox.Foreground>
                <SolidColorBrush Color="Gray"/>
            </TextBox.Foreground>
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter Property="Text" Value="Username"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>

        <PasswordBox x:Name="PasswordBox" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Margin="10,100,10,10">
            <PasswordBox.Foreground>
                <SolidColorBrush Color="Gray"/>
            </PasswordBox.Foreground>
            <PasswordBox.Style>
                <Style TargetType="PasswordBox">
                    <Style.Triggers>
                        <Trigger Property="Password" Value="">
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter Property="Password" Value="Password"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </PasswordBox.Style>
        </PasswordBox>
        
        <Button Content="Login" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="100" Height="30" Margin="10" Command="{Binding LoginCommand}"/>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10,0,10,40" Foreground="Red" Text="{Binding ErrorMessage}"/>
    </Grid>
</Window>
